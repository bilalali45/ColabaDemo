export class TemplateDocument {
    public docId: string;
    public docName: string;

    constructor(docId: string, docName: string) {
        this.docId = docId;
        this.docName = docName;
    }
}
