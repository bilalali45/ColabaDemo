import {LoanEndpoints} from './loan/LoanEndpoints'
import {DocumentsEndpoints} from './documents/DocumentsEndpoints'

export class Endpoints {
    static loan = LoanEndpoints;
    static documents = DocumentsEndpoints;
}